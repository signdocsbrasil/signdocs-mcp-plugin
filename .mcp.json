{
  "mcpServers": {
    "signdocs": {
      "type": "http",
      "url": "${user_config.endpoint_url}",
      "headers": {
        "X-SignDocs-Client-Id": "${user_config.client_id}",
        "X-SignDocs-Client-Secret": "${user_config.client_secret}"
      }
    }
  }
}
