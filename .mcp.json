{
  "mcpServers": {
    "signdocs": {
      "type": "http",
      "url": "${user_config.endpoint_url}",
      "headers": {
        "Authorization": "Basic ${user_config.basic_auth}"
      }
    }
  }
}
